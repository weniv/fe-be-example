# 데이터베이스 설정
# 실제 사용 시에는 이 파일을 .env로 복사하고 실제 값으로 변경하세요

# 로컬 개발용 SQLite 데이터베이스
DATABASE_URL=sqlite:///./todos.db

# AWS Lightsail 데이터베이스 예시 (실제 값으로 변경 필요)
# DATABASE_URL=postgresql://username:password@database-host:port/database_name

# 데이터베이스 사용자명 (PostgreSQL 사용 시)
DB_USER=your_username

# 데이터베이스 비밀번호 (PostgreSQL 사용 시)
DB_PASSWORD=your_password

# 데이터베이스 호스트 (PostgreSQL 사용 시)
DB_HOST=your_lightsail_db_host

# 데이터베이스 포트 (PostgreSQL 사용 시)
DB_PORT=5432

# 데이터베이스 이름 (PostgreSQL 사용 시)
DB_NAME=todos_db